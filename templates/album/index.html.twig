<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mon Album Photo</title>
</head>
<body>
    <h2>Mon Album Photo</h2>

    <div>
        {% for photo in photos %}
            <div style="margin-bottom: 20px;">
                <h2>{{ photo.title }}</h2>
                <img src="{{ asset('uploads/photos/' ~ photo.filename) }}" alt="{{ photo.title }}" style="max-width: 300px;">
            </div>
        {% else %}
            <p>Aucune photo à afficher.</p>
        {% endfor %}
    </div>
</body>
</html>
