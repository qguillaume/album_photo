{% extends 'base.html.twig' %}

{% block body %}
    <h2>Créer un nouvel album</h2>

    {{ form_start(form) }}
        <div class="form-group">
            {{ form_widget(form.nomAlbum) }}
        </div>
        <div class="form-group">
            {{ form_widget(form.image_path) }}
        </div>
        <div class="form-group">
            {{ form_widget(form.create_album) }}
        </div>
    {{ form_end(form) }}
{% endblock %}