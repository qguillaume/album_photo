<?php

namespace App\Tests\Controller;

use Symfony\Bundle\FrameworkBundle\Test\WebTestCase;

class PhotoControllerTest extends WebTestCase
{
    public function testUploadPhoto(): void
    {/*
       $client = static::createClient();
       $crawler = $client->request('GET', '/photo/upload');

       // Vérifie que la page est accessible
       $this->assertResponseIsSuccessful();

       // Vérifie la présence d'un formulaire avec un champ de type "file"
       $this->assertSelectorExists('form input[type="file"]');

       // Vérifie la présence d'un bouton de soumission
       $this->assertSelectorExists('form button[type="submit"]');*/
    }
}
